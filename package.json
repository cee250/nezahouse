{
  "name": "grandchaletinn-static-site",
  "private": true,
  "version": "1.0.0",
  "description": "Grand Chalet Inn static site with booking email notifications",
  "scripts": {
    "build": "python3 scripts/prepare_assets.py",
    "start": "python3 -m http.server 8000 --directory site"
  },
  "dependencies": {
    "nodemailer": "^10.0.0"
  }
}
